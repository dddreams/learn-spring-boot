server:
  port: 8080

spring:
  application:
    name: spring-boot-jdbc

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: shure
    password: shure
